name: Gofmt

on:
  pull_request:
    types: [open, reopened, edited]
    paths:
      - '**.go'

jobs:
  build:
    runs-on: [ self-hosted ]
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21.1'
    - name: Check lint
      run: go fmt .
